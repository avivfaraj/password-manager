services:
  password-manager:
    build: .
    image: pm:dev
    container_name: password-manager
    environment:
      - DISPLAY=${DISPLAY_IP:-host.docker.internal}:1
    volumes:
    - ./data:/app/data
    stdin_open: true
    tty: true